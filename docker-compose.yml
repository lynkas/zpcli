version: '3.8'

services:
  zpcli-mcp:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/etc/zpcli
    environment:
      - ZPCLI_CONFIG=/etc/zpcli/sites.json
    restart: always
